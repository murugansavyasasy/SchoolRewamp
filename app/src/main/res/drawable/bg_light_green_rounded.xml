<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Left dark green strip with rounded left corners -->
    <item
        android:width="6dp"
        android:gravity="start">
        <shape android:shape="rectangle">
            <solid android:color="@color/PrimaryColor" />
            <corners
                android:bottomLeftRadius="12dp"
                android:topLeftRadius="12dp" />
        </shape>
    </item>

    <!-- Main light green background with rounded corners -->
    <item android:left="6dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/PrimaryColor_80" />
            <corners
                android:bottomRightRadius="12dp"
                android:topRightRadius="12dp" />
        </shape>
    </item>

</layer-list>
    <!--Note-Important -->
    <!--Try to Read it fully-->
    <!--See  when you are changing this drawable make sure to change the idrtl file also because in arabic that change should be reflect or else the UI will be different-->

