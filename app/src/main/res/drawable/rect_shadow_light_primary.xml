<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Shadow/background layer -->
    <item android:left="2dp" android:top="2dp">
        <shape>
            <solid android:color="@color/PrimaryColor" /> <!-- Simulated shadow -->
            <corners android:radius="8dp" />
        </shape>
    </item>

    <!-- Actual button layer -->
    <item>
        <shape>
            <solid android:color="@color/PrimaryColor" />
            <corners android:radius="8dp" />
            <stroke
                android:width="1dp"
                android:color="@color/PrimaryColor" /> <!-- Slight border for depth -->
        </shape>
    </item>

</layer-list>
