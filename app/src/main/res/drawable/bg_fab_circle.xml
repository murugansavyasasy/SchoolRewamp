<?xml version="1.0" encoding="utf-8"?>
<!-- Place this file at: res/drawable/bg_fab_circle.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <solid android:color="@color/PrimaryColor" />

    <!-- Optional: gives it a subtle border. Remove the <stroke> block if you don't want it. -->
    <stroke
        android:width="0dp"
        android:color="@android:color/transparent" />

</shape>